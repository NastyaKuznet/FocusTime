<resources>
    <!-- Base application theme (light). -->
    <style name="Base.Theme.FocusTime" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- это в навигатион -->
        <item name="colorPrimary">@color/light_green_darker</item>
        <!-- вариант нажатой кнопки -->
        <item name="colorPrimaryVariant">@color/light_green_darker</item>
        <!-- верхняя плашка -->
        <item name="android:statusBarColor">@color/light_green_darker</item>
        <!-- Устанавливаем цвет текста для Toolbar -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <!-- Устанавливаем цвет иконок для Toolbar -->
        <item name="colorControlNormal">@android:color/black</item>

        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="Theme.FocusTime" parent="Base.Theme.FocusTime" />

</resources>

